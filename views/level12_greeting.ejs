<!DOCTYPE html>
<html lang="hr">
<head>
  <meta charset="UTF-8">
  <title>Pozdrav</title>
</head>
<body style="font-family: sans-serif; padding: 40px; text-align: center;">
  <h1>👋 Bok, <%= name %>!</h1>

  <p>Dobrodošao na razinu 12.</p>

  <% if (name.includes("flag")) { %>
    <form method="POST" action="/level12/verify">
      <input type="hidden" name="flagInput" value="<%= flag %>">
      <button type="submit" style="margin-top: 20px; padding: 10px 20px;">Završi razinu</button>
    </form>
  <% } %>
</body>
</html>
